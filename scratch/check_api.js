const fetch = require('node-fetch');

async function checkApi() {
  const token = 'YOUR_TOKEN'; // I don't have the token, but I can try to get it from localStorage if I were a browser.
  // Since I'm on the server, I'll try to call the service directly via node if possible, 
  // or just trust that my backend code is correct.
}
